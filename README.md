# InventarioDeMaquinas

Inventario de Máquinas con Angular, NextJS, TypeORM y PostgreSQL

1. Ejecutar el comando para crear el docker con la base de postgresql que se utilizará
   > docker-compose up -d
